<% current_user.activities.where(trackable_type: Responce, action: 'create').group_by{|e| e.trackable.post.id}.each do |parent, ecategories| %>
				<%= ecategories.count %> новых предложений к заданию
                <%= link_to Post.find(parent).name, Post.find(parent) %>
              <% end %>